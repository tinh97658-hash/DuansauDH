# Frontend test suite

- `unit/`: isolated functions and configuration.
- `integration/`: components rendered with their router/provider boundaries.

Keep tests under `src/` so Create React App can transform and execute them. Prefer behavior visible to users over implementation details.
